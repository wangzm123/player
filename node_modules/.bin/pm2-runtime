#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pm2/bin/pm2-runtime" "$@"
  ret=$?
else 
  node  "$basedir/../pm2/bin/pm2-runtime" "$@"
  ret=$?
fi
exit $ret
